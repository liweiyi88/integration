machine:
  timezone:
    Australia/Melbourne
  php:
    version: 7.0.4

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy --trace
      - bundle exec cap production deploy:migrate --trace